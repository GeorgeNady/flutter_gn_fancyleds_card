name: flutter_gn_fancyleds_card
description: "A customizable Flutter card widget with an animated LED border and vibrant glow effects."
version: 0.0.1
homepage:

environment:
  sdk: ^3.10.9
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter: